{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev-install-frontend":"cd frontend && npm install",
    "dev-install-server":"npm install",
    "dev-frontend":"cd frontend && npm run serve",
    "dev-server":"nodemon server.js",
    "dev": "concurrently npm:dev-*",
    "prod-intstall":"cd frontend && npm install",
    "prod-build":" cd frontend && npm run build",
    "prod-server":"npm install && nodemon server.js",
    "start":"concurrently npm:prod-*"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^6.4.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongodb": "^4.2.0",
    "mongoose": "^6.0.13",
    "nodemon": "^2.0.15"
  }
}
